{
  "name": "caskdb",
  "version": "1.0.0",
  "description": "CaskDB is a disk-based, embedded, persistent, key-value store based on the [Riak's bitcask paper](https://riak.com/assets/bitcask-intro.pdf), written in Node.js.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest --pool=forks --run",
    "build": "tsc",
    "bench": "tsc && node --expose-gc dist/bench.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vite": "^6.0.0",
    "vitest": "^4.1.6",
    "tsx": "^4.22.0"
  }
}
